<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p>Completed: <%= @task.completed ? '✔︎ ' : '✘ ' %></p>

<p><%= link_to "See all tasks", tasks_path %></p>
<p><%= link_to "Edit task", edit_task_path(@task) %></p>
<p><%= link_to "Delete task", task_path(@task), method: :delete %></p>
